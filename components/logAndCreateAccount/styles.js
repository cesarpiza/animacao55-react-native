import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        position: 'absolute',
        bottom: 90,
        flexDirection: 'row',
        alignSelf: 'center',
    },
    text: {
        backgroundColor: '#FFF',
        padding: 20,
        borderRadius: 10,
        fontSize: 18,
        fontWeight: 'bold',
        marginHorizontal: 20,
    },
});